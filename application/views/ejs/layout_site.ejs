
<!doctype html>
<head>
    <meta charset=utf-8>
    <title>Nested Layouts Demo</title>
    <%- css ('header')  %>
    <%- js ('ejs')  %>
    <%- js ('index')  %>
    <script src="/socket.io/socket.io.js"></script>
    <script>
      var socket = io.connect('http://localhost');
      socket.on('news', function (data) {

        // console.log ( data.changed, data.layout, data.options );
        // socket.emit('my other event', { my: 'data' });
        if ( data.changed )
        {
            layoutUpdate ( data.changed, data.layout, data.options );
            // document.body.innerHTML = data.options.body;
        }
      });
    </script>
</head>
<body>
    <h1>Site EJS Layout</h1>
    <p>
        This is the master site layout.
        It is not wrapped by a layout.
    </p>

    <%- body %>

    <%- partial ( "part", { title: "Hello", contents: "adasd"} ) %>
    <%- partial ( "part", { title: "Hello", contents: "adasd"} ) %>
</body>
</html>
